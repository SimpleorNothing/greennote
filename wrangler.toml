name = "greennote"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

# R2 버킷을 Functions에 바인딩한다.
# binding = 코드에서 env.greennote 로 접근하는 이름
# bucket_name = 실제 R2 버킷 이름
[[r2_buckets]]
binding = "greennote"
bucket_name = "greennote"
